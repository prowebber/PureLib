{
  "name": "purelib",
  "version": "1.2.0",
  "description": "PureLib a light-weight performant JavaScript library",
  "main": "src/index.js",
  "scripts": {
    "archive": "gulp --gulpfile archive-version.js",
    "build": "rollup -c rollup.purelib.js && terser dist/purelib.js -c -m keep_classnames -o dist/purelib.min.js",
    "build_prev": "rollup -c rollup.config.js && terser build/purelib.js -c -m keep_classnames -o build/purelib.min.js"
  },
  "author": "prowebber",
  "license": "MIT",
  "dependencies": {},
  "babel": {
    "presets": [
      "@babel/env"
    ]
  }
}
